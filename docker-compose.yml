version: '3'
services:
  postgres:
    build:
      context: ./server
      dockerfile: Dockerfile-database
  server:
    build: 
      context: ./server
      dockerfile: Dockerfile
    depends_on:
      - postgres
  app:
    build:
      content: ./app
      dockerfile: Dockerfile